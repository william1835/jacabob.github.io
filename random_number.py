#Import random modual
import random

#Creates variable number and sets it equal to a random number from 1 to 10 inclusive
number = random.randint(1, 10)

#Outputs the number so the user can see it
print(number)